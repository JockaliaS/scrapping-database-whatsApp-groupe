# AGENT 5 — DevOps / MCP

## Responsabilites
- Dockerfiles multi-stage (backend Rust + frontend React/Nginx)
- docker-compose.yml compatible Coolify (pas de labels Traefik, pas de reseaux custom)
- nginx.conf pour le SPA routing
- Serveur MCP avec 7 outils de test/monitoring
- Configuration .env.example
